target-version = "py311"
line-length = 120

[lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "A", "C4"]
# B008: FastAPI 的 Depends()/File()/Form() 在参数默认值中是标准模式，非误用
ignore = ["E501", "B008"]

[lint.per-file-ignores]
"__init__.py" = ["F401"]
"tests/*" = ["S101"]
